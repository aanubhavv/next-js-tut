'use client' // defines that this component is client side component

const Hello = () => {
    console.log('I am a client component')
    return (
        <div>Hello</div>
    )
}

export default Hello